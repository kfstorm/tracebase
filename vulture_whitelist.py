decode_path_id  # Used by archive readers outside the current CLI.
_.write_snapshot  # Used by source collectors as they are implemented.
_.write_evidence  # Used by source collectors as they are implemented.
_.publish  # Used by source collectors as they are implemented.
_.tokens_input  # Read dynamically when aggregating OpenCode session metrics.
_.tokens_output  # Read dynamically when aggregating OpenCode session metrics.
_.tokens_reasoning  # Read dynamically when aggregating OpenCode session metrics.
_.tokens_cache_read  # Read dynamically when aggregating OpenCode session metrics.
_.tokens_cache_write  # Read dynamically when aggregating OpenCode session metrics.
